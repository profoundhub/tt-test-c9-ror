json.extract! friend, :id, :name, :code, :link, :aka, :vip, :fps, :floors, :daily, :active, :rebuild, :requests, :time, :notes, :index, :show, :new, :created_at, :updated_at
json.url friend_url(friend, format: :json)